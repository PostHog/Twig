procs:
  agent:
    cmd: ["pnpm", "--filter", "agent", "run", "dev"]
    cwd: .
    color: magenta

  array:
    cmd: ["pnpm", "--filter", "array", "run", "start"]
    cwd: .
    color: blue
    depends_on:
      - agent